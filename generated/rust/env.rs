// Generated by flags-2-env from .cli-flags.toml. Do not edit.
#![allow(dead_code)]

pub const FIDUCIA_FLAGS_CONFIG: &str = "FIDUCIA_FLAGS_CONFIG";
pub const JSON: &str = "FIDUCIA_JSON";
pub const JSON_DEFAULT: &str = "false";
pub const ONLY: &str = "FIDUCIA_ONLY_REGION";
pub const PATH: &str = "FIDUCIA_HEALTH_PATH";
pub const PATH_DEFAULT: &str = "/healthz";
pub const REGIONS: &str = "FIDUCIA_REGIONS_FILE";
pub const REGIONS_DEFAULT: &str = "edge-regions.json";
pub const SAMPLES: &str = "FIDUCIA_SAMPLES";
pub const SAMPLES_DEFAULT: &str = "5";
pub const TIMEOUT: &str = "FIDUCIA_TIMEOUT_MS";
pub const TIMEOUT_DEFAULT: &str = "2000";
pub const WARMUP: &str = "FIDUCIA_WARMUP";
pub const WARMUP_DEFAULT: &str = "0";
pub const SHELL: &str = "FIDUCIA_COMPLETION_SHELL";
pub const SHELL_DEFAULT: &str = "bash";
pub const URL: &str = "FIDUCIA_NODE_URL";

/// Compile-time env key names from `.cli-flags.toml`.
pub struct CliEnv {
    /// Runtime environment key FIDUCIA_FLAGS_CONFIG.
    pub fiducia_flags_config: &'static str,
    /// Emit machine-readable JSON instead of the human table.
    pub json: &'static str,
    /// Probe only the region with this name (unset means all regions).
    pub only: &'static str,
    /// Absolute health path probed on each region endpoint.
    pub path: &'static str,
    /// Path to the regions JSON ([{name,url}], as generated by fiducia-infra).
    pub regions: &'static str,
    /// Number of latency probes per region (1 through 100).
    pub samples: &'static str,
    /// Per-probe timeout in milliseconds (1 through 60000).
    pub timeout: &'static str,
    /// Discard the first N probes per region (0 through 100).
    pub warmup: &'static str,
    /// Shell dialect to emit: bash or zsh.
    pub shell: &'static str,
    /// Probe this node URL directly instead of resolving one from --regions.
    pub url: &'static str,
}

impl CliEnv {
    pub const KEYS: Self = Self {
        fiducia_flags_config: FIDUCIA_FLAGS_CONFIG,
        json: JSON,
        only: ONLY,
        path: PATH,
        regions: REGIONS,
        samples: SAMPLES,
        timeout: TIMEOUT,
        warmup: WARMUP,
        shell: SHELL,
        url: URL,
    };
}
