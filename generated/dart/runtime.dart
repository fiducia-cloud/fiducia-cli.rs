// Generated by flags-2-env from .cli-flags.toml. Do not edit.

import 'dart:io';

final class CliEnvValues {
  const CliEnvValues({required this.fiduciaFlagsConfig, required this.json, required this.only, required this.path, required this.regions, required this.samples, required this.timeout, required this.warmup, required this.shell, required this.url});

  final String? fiduciaFlagsConfig;
  final bool json;
  final String? only;
  final String path;
  final String regions;
  final int samples;
  final int timeout;
  final int warmup;
  final String shell;
  final String? url;
}

/// Pure: resolve values from an explicit lookup.
CliEnvValues loadFrom(String? Function(String key) lookup) {
  return CliEnvValues(
    fiduciaFlagsConfig: _nonEmpty(lookup('FIDUCIA_FLAGS_CONFIG')),
    json: _parseBool(lookup('FIDUCIA_JSON'), false),
    only: _nonEmpty(lookup('FIDUCIA_ONLY_REGION')),
    path: _nonEmpty(lookup('FIDUCIA_HEALTH_PATH')) ?? '/healthz',
    regions: _nonEmpty(lookup('FIDUCIA_REGIONS_FILE')) ?? 'edge-regions.json',
    samples: _parseInt(lookup('FIDUCIA_SAMPLES'), 5),
    timeout: _parseInt(lookup('FIDUCIA_TIMEOUT_MS'), 2000),
    warmup: _parseInt(lookup('FIDUCIA_WARMUP'), 0),
    shell: _nonEmpty(lookup('FIDUCIA_COMPLETION_SHELL')) ?? 'bash',
    url: _nonEmpty(lookup('FIDUCIA_NODE_URL')),
  );
}

/// Resolve values from a map of env keys.
CliEnvValues loadFromMap(Map<String, String> env) {
  return loadFrom((key) => env[key]);
}

/// Effectful: read the process environment.
CliEnvValues loadFromOs() {
  return loadFromMap(Platform.environment);
}

String? _nonEmpty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

bool _parseBool(String? raw, bool fallback) {
  switch (raw?.trim()) {
    case '1':
    case 'true':
    case 'TRUE':
    case 'yes':
    case 'YES':
      return true;
    case '0':
    case 'false':
    case 'FALSE':
    case 'no':
    case 'NO':
      return false;
    default:
      return fallback;
  }
}

int _parseInt(String? raw, int fallback) {
  return int.tryParse(raw?.trim() ?? '') ?? fallback;
}

double _parseDouble(String? raw, double fallback) {
  return double.tryParse(raw?.trim() ?? '') ?? fallback;
}

String? _nonempty(String? raw) {
  final value = raw?.trim();
  if (value == null || value.isEmpty) {
    return null;
  }
  return value;
}

String requireEnv(String name, String expectedType, List<String> examples, String? value) {
  final trimmed = _nonempty(value);
  if (trimmed != null) {
    return trimmed;
  }
  throw MissingEnv(name: name, expectedType: expectedType, examples: examples);
}

String? pick(
  List<String> keys,
  List<String> order,
  Map<String, String> shell,
  Map<String, String> dotenv,
  Map<String, String> flags,
  String? fallback,
) {
  for (final source in order) {
    final map = source == 'flags' ? flags : source == 'env_file' ? dotenv : shell;
    for (final key in keys) {
      final value = _nonempty(map[key]);
      if (value != null) {
        return value;
      }
    }
  }
  return _nonempty(fallback);
}

String _unquote(String value) {
  final trimmed = value.trim();
  if (trimmed.length >= 2 &&
      ((trimmed.startsWith('"') && trimmed.endsWith('"')) ||
          (trimmed.startsWith("'") && trimmed.endsWith("'")))) {
    return trimmed.substring(1, trimmed.length - 1);
  }
  return trimmed;
}

Map<String, String> parseDotenv(String text) {
  final out = <String, String>{};
  for (final raw in text.split('\n')) {
    var line = raw.trim();
    if (line.isEmpty || line.startsWith('#')) {
      continue;
    }
    if (line.startsWith('export ')) {
      line = line.substring('export '.length).trim();
    }
    final eq = line.indexOf('=');
    if (eq <= 0) {
      continue;
    }
    final key = line.substring(0, eq).trim();
    if (key.isEmpty) {
      continue;
    }
    out[key] = _unquote(line.substring(eq + 1));
  }
  return out;
}

bool _dotenvEnabled() {
  final value = Platform.environment['FLAGS2ENV_DOTENV'];
  return value != '0' && value != 'false' && value != 'FALSE' && value != 'no' && value != 'NO';
}

Map<String, String> loadDotenvFiles(List<String> files) {
  if (!_dotenvEnabled()) {
    return {};
  }
  return files.fold<Map<String, String>>({}, (acc, path) {
    try {
      acc.addAll(parseDotenv(File(path).readAsStringSync()));
    } catch (_) {}
    return acc;
  });
}
final class MissingEnv implements Exception {
  const MissingEnv({required this.name, required this.expectedType, required this.examples});
  final String name;
  final String expectedType;
  final List<String> examples;
  @override
  String toString() => 'missing required environment variable $name\n  expected type: $expectedType\n  examples: ${examples.join(', ')}';
}

Map<String, String> loadEnvMap(
  Map<String, String> shell,
  Map<String, String> dotenv, [
  Map<String, String> flags = const {},
]) {
  final out = <String, String>{};
  final fiduciaFlagsConfig = pick(['FIDUCIA_FLAGS_CONFIG'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (fiduciaFlagsConfig != null) out['FIDUCIA_FLAGS_CONFIG'] = fiduciaFlagsConfig;
  final json = pick(['FIDUCIA_JSON'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'false');
  if (json != null) out['FIDUCIA_JSON'] = json;
  final only = pick(['FIDUCIA_ONLY_REGION'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (only != null) out['FIDUCIA_ONLY_REGION'] = only;
  final path = pick(['FIDUCIA_HEALTH_PATH'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '/healthz');
  if (path != null) out['FIDUCIA_HEALTH_PATH'] = path;
  final regions = pick(['FIDUCIA_REGIONS_FILE'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'edge-regions.json');
  if (regions != null) out['FIDUCIA_REGIONS_FILE'] = regions;
  final samples = pick(['FIDUCIA_SAMPLES'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '5');
  if (samples != null) out['FIDUCIA_SAMPLES'] = samples;
  final timeout = pick(['FIDUCIA_TIMEOUT_MS'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '2000');
  if (timeout != null) out['FIDUCIA_TIMEOUT_MS'] = timeout;
  final warmup = pick(['FIDUCIA_WARMUP'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, '0');
  if (warmup != null) out['FIDUCIA_WARMUP'] = warmup;
  final shell = pick(['FIDUCIA_COMPLETION_SHELL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, 'bash');
  if (shell != null) out['FIDUCIA_COMPLETION_SHELL'] = shell;
  final url = pick(['FIDUCIA_NODE_URL'], ['flags', 'env_shell', 'env_file'], shell, dotenv, flags, null);
  if (url != null) out['FIDUCIA_NODE_URL'] = url;
  return out;
}

const List<String> _dotenvFiles = ['.env'];

Map<String, String> loadEnvMapFromOs() {
  return loadEnvMap(Platform.environment, loadDotenvFiles(_dotenvFiles));
}
